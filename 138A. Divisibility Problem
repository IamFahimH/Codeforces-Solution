#include <iostream>
#include <cmath>
using namespace std;
int main() {
    long long int tst,count;
    cin>>tst;
    while(tst--){
        count=0;
        long long int a,b;
        cin>>a>>b;
        if(a%b==0)cout<<"0"<<endl;
        else{
            /*while(!(a%b==0)){
                a++; count++;*/
            cout<<b-(a%b)<<endl;
            }
        }
    return 0;
}
