#include <iostream>
#include <cmath>
using namespace std;
int main() {
    int n,k; cin>>n>>k;

    int var,count=0;
    for(int i=0;i<n;i++){
        cin>>var;
        if(var+k<=5){
            count++;
        }
    }

    cout<<count/3<<endl;
    return 0;
}
