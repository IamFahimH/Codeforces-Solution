#include <iostream>
#include <cmath>
#define rocket ios::sync_with_stdio(false);cin.tie(0);cout.tie(0)
typedef long long int ll;
using namespace std;
int main(){
    rocket;
    ll n; cin>>n;
    if(n%2==1){cout<<0<<endl; return 0;}
    cout<<(ll)(pow(2,n/2))<<endl;
    return 0;
}
