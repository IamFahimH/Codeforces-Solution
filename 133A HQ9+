#include <iostream>
#include <vector>
#include <cstring>
using namespace std;
int main(){
    char word[100];
    cin>>word;
    for(int i=0;i<strlen(word);i++){
        if(word[i]=='H' || word[i]=='Q' || word[i]=='9'){
            cout<<"YES"<<endl;
            return 0;
        }
    }
    cout<<"NO"<<endl;
    return 0;
}
