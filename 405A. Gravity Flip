#include <iostream>
#include <algorithm>
using namespace std;
int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    int box[100],n; cin>>n;
    for(int i=0;i<n;i++)
        cin>>box[i];

    sort(box,box+n);
    for(int i=0;i<n;i++)
        cout<<box[i]<<" ";
    return 0;
}
